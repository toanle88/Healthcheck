name: "🏁 Full Stack Pipeline"

on:
  push:
    branches: [ main ]
    paths-ignore:
      - 'infra/**'
      - 'docs/**'
      - '**.md'
  pull_request:
    branches: [ main ]
    paths-ignore:
      - 'infra/**'
      - 'docs/**'
      - '**.md'
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  # ==========================================
  # STAGE 1: THE AUDIT (CI)
  # ==========================================
  audit:
    name: "🛡️ Audit: Quality & Security"
    runs-on: ubuntu-latest
    steps:
      - name: "📂 Checkout Code"
        uses: actions/checkout@v4

      - name: "🐹 Setup Go Environment"
        uses: actions/setup-go@v5
        with:
          go-version: '1.26'

      # -------------------------------------------------
      # Cache Go module downloads (~/.cache/go/mod) and
      # the build cache (~/.cache/go/build) so subsequent
      # runs skip re-downloading dependencies.
      # -------------------------------------------------
      - name: "📦 Cache Go Modules"
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/go/mod
            ~/.cache/go/build
          # Key is based on OS + go.sum so the cache is invalidated
          # whenever a dependency changes.
          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-

      - name: "📏 Verify Code Formatting"
        run: if [ "$(gofmt -l . | wc -l)" -gt 0 ]; then exit 1; fi

      - name: "🔍 Static Analysis (Vet)"
        run: go vet ./...

      - name: "⚡ Run Unit Tests"
        run: go test -v -race ./...

      - name: "🔒 Security Scan (Trivy)"
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'fs'
          ignore-unfixed: true
          format: 'table'
          severity: 'CRITICAL,HIGH'

  # ==========================================
  # STAGE 3: THE FACTORY (Build & Push)
  # ==========================================
  build:
    name: "📦 Build: Docker Images"
    needs: audit # <--- ONLY RUN IF AUDIT PASSES!
    if: github.event_name != 'pull_request' # Don't build/push on PRs, only on push/manual
    runs-on: ubuntu-latest
    outputs:
      image_tag: ${{ steps.vars.outputs.sha_short }}
    steps:
      - name: "📂 Checkout Code"
        uses: actions/checkout@v4

      - name: "🆔 Generate Version Tag"
        id: vars
        run: echo "sha_short=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT

      - name: "🔑 Login to Azure (OIDC)"
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: "🔓 Login to Container Registry"
        run: az acr login --name ${{ secrets.AZURE_ACR_NAME }}

      - name: "🐳 Build & Push API Image"
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile.api
          push: true
          tags: ${{ secrets.AZURE_ACR_NAME }}.azurecr.io/api:${{ steps.vars.outputs.sha_short }}
          build-args: APP_VERSION=${{ steps.vars.outputs.sha_short }}

      - name: "🐝 Build & Push Worker Image"
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile.worker
          push: true
          tags: ${{ secrets.AZURE_ACR_NAME }}.azurecr.io/worker:${{ steps.vars.outputs.sha_short }}
          build-args: APP_VERSION=${{ steps.vars.outputs.sha_short }}

      - name: "🌐 Build & Push Web Image"
        uses: docker/build-push-action@v5
        with:
          context: ./web
          file: ./web/Dockerfile.web
          push: true
          tags: ${{ secrets.AZURE_ACR_NAME }}.azurecr.io/web:${{ steps.vars.outputs.sha_short }}

  # ==========================================
  # STAGE 4: THE DEPLOYER (Fast Update)
  # ==========================================
  deploy:
    name: "🚀 Deploy: Fast CLI Update"
    needs: build # <--- ONLY RUN IF BUILD PASSES!
    runs-on: ubuntu-latest
    # Expose the API URL so the smoke-test job can consume it
    outputs:
      api_url: ${{ steps.get_url.outputs.api_url }}
    steps:
      - name: "🔑 Login to Azure (OIDC)"
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: "⚡ Update API Container"
        run: |
          az containerapp update \
            --name ca-healthcheck-api-dev \
            --resource-group rg-healthcheck-dev \
            --image ${{ secrets.AZURE_ACR_NAME }}.azurecr.io/api:${{ needs.build.outputs.image_tag }}

      - name: "⚡ Update Web Container"
        run: |
          az containerapp update \
            --name ca-healthcheck-web-dev \
            --resource-group rg-healthcheck-dev \
            --image ${{ secrets.AZURE_ACR_NAME }}.azurecr.io/web:${{ needs.build.outputs.image_tag }} \
            --set-env-vars VITE_APP_VERSION=${{ needs.build.outputs.image_tag }}

      - name: "⚡ Update Worker Job"
        run: |
          az containerapp job update \
            --name caj-healthcheck-worker-dev \
            --resource-group rg-healthcheck-dev \
            --image ${{ secrets.AZURE_ACR_NAME }}.azurecr.io/worker:${{ needs.build.outputs.image_tag }}

      # Fetch the live FQDN after update so the smoke-test job can use it
      - name: "🔍 Get API URL"
        id: get_url
        run: |
          FQDN=$(az containerapp show \
            --name ca-healthcheck-api-dev \
            --resource-group rg-healthcheck-dev \
            --query "properties.configuration.ingress.fqdn" \
            --output tsv)
          echo "api_url=https://${FQDN}" >> $GITHUB_OUTPUT

  # ==========================================
  # STAGE 5: POST-DEPLOY SMOKE TEST
  # Hits /health and fails the pipeline if the
  # live endpoint is not responding correctly.
  # ==========================================
  smoke-test:
    name: "🔥 Smoke Test: /health Check"
    needs: deploy # <--- ONLY RUN AFTER SUCCESSFUL DEPLOY
    runs-on: ubuntu-latest
    steps:
      - name: "🌡️ Hit /health Endpoint"
        run: |
          API_URL="${{ needs.deploy.outputs.api_url }}"
          echo "Smoke-testing: ${API_URL}/health"

          # Retry for up to 90 s to let the new revision warm up
          for attempt in $(seq 1 9); do
            HTTP_STATUS=$(curl --silent --output /dev/null \
              --write-out "%{http_code}" \
              --max-time 10 \
              "${API_URL}/health")

            echo "Attempt ${attempt}/9 → HTTP ${HTTP_STATUS}"

            if [ "${HTTP_STATUS}" -eq 200 ]; then
              echo "✅ Smoke test passed (HTTP 200)"
              exit 0
            fi

            sleep 10
          done

          echo "❌ Smoke test failed — /health did not return 200 after 90 s"
          exit 1
